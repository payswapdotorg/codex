# WO-001 — Repository & Agent Bootstrap

**Status:** ACTIVE
**Base:** `4b0d9669cc46ba97bf85fa6312431b630d80498d`
**Primary surface:** architecture and implementation governance documentation.

## Objective

Prepare `payswapdotorg/codex` so autonomous coding agents can safely work from repository truth while the project evolves toward a model-independent Codex runtime with a first-class workflow layer.

## Allowed changes

- `ARCHITECT_START_HERE.md`
- `docs/architecture/**`
- `docs/development-state/**`
- `docs/work-orders/**`
- root `AGENTS.md` only when needed to point agents to the authoritative bootstrap contract
- root `README.md` only for accurate project identity/navigation

## Forbidden changes

- Codex runtime behavior
- provider implementation behavior
- authentication behavior
- sandbox behavior
- CLI/TUI behavior
- workflow runtime implementation
- dependency upgrades unrelated to documentation bootstrap

## Required outcomes

1. Architect start document exists and identifies authority, current phase, and non-negotiable boundaries.
2. Frozen architecture document defines Universal Model Plane and Workflow Plane boundaries.
3. Architecture lock identifies what may not drift and how changes are authorized.
4. Machine-readable development state and dependency graph exist.
5. Work Order protocol exists.
6. No runtime behavior is changed.

## Verification

- Inspect Git diff and confirm only allowed surfaces changed.
- Verify architecture documents reference the actual current base SHA.
- Verify JSON files parse successfully.
- Verify no provider/runtime source files changed.

## Completion report

Return base SHA, head SHA, changed files, JSON validation evidence, architecture-surface summary, and confirmation that no runtime behavior changed.
