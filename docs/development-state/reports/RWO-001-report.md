# RWO-001 Completion Report (filed by Tech Lead)

**Work Order:** RWO-001 — Mount the workflow/teaching control plane behind user-facing surfaces (VWO-010 Family A — the single root finding of the validation program, 6 report confirmations)
**Session:** chat.z.ai agents-tab, GLM-5.3 + Full-Stack (mount worker chat 349d3e60 lineage; RWO-001-FIX follow-up chat 7b471592-3b74-4725-ac7c-4e12e02b6c7d)
**Dispatched:** 2026-09-12 (mount) · **RWO-001-FIX dispatched:** 2026-09-13 12:14 UTC · **Merged:** 2026-09-13 19:24 UTC via PR #35 (squash, df8261b23c)
**Delivery:** git bundles (mount ab3bb1860; fix RWO-001-FIX-delivery.bundle head 772baceb19e8f2b2b96d07420b9fff204555f7d6, exactly 1 commit over d003b025e)

## Lineage

- `ab3bb1860` — worker mount: additive protocol methods (app-server-protocol), app-server handlers delegating to EXISTING engine ports, one CLI workflow subcommand group. No new engine semantics; frozen engine crates untouched.
- `d003b025e` — Tech-Lead integration fixes: compile-level corrections, test-fixture sha256 typo, `WorkflowTriggerSource.event_id` skip_serializing_if, experimental precomputed .zst regenerated by the real generator.
- `772baceb1` — RWO-001-FIX (same implementation specialist): three deterministic service-test failures fixed implementation-side; `workflow.rs` only (+110/-17); tests byte-identical.

## RWO-001-FIX Worker's Completion Report (verbatim, harvested from the live session DOM)

=== RWO-001-FIX COMPLETION REPORT ===

Work Order ID: RWO-001-FIX

base branch + base SHA: rwo-001/mount-control-plane @ d003b025ea2095447d2919ba6a77ee676dfacd12 (verified reachable; branch tip == d003b025e; fix branch rwo-001-fix/three-test-failures cut from it)

head SHA (your branch, after your commit): 772baceb19e8f2b2b96d07420b9fff204555f7d6 (exactly ONE commit on top of d003b025e; bundle: /home/z/my-project/RWO-001-FIX-delivery.bundle, created with git bundle create … d003b025e..rwo-001-fix/three-test-failures, git bundle verify OK, NOT pushed)

changed files/surfaces: codex-rs/app-server/src/workflow.rs only (+110/−17). Byte-identical (0-line diffs vs d003b025e): the three tests (workflow_tests.rs), all frozen engine crates (codex-teaching-compiler, codex-workflow-*, codex-execution-contracts), app-server-protocol, cli, and all docs/validation/contract surfaces. No credentials anywhere in the diff.

root-cause diagnosis for each of the three failures:

Hybrid origin ordering (teach_compile_review_approve_publish_in_all_three_modes, assert at workflow_tests.rs:123→126): the frozen teaching compiler assigns step-001, step-002, … strictly in trajectory recording order (blueprint.rs: "Extraction never reorders or merges records"), and the mount's compile() fed it the raw session — where the Hybrid flow records instruct-then-demonstrate — so step-001 came out Instructed while the VWO-010 Family A reconciled hybrid origins are ["observed","instructed"] (observed first). The reconcile/merge policy is the control plane's responsibility. Fix: compile() now compiles Hybrid sessions through reconciled_hybrid_session() — a session view replayed through the frozen crate's public append-only API with all demonstration (observed) records before all instruction (instructed) records, relative order preserved per group (so observation/action/result pairing survives); Demonstrate/Instruct sessions pass through unchanged (single origin ⇒ recording order already is the reconciled order).

Publish input-validation vs state-gate ordering (publish_validates_the_identity_inputs, assert at workflow_tests.rs:255): publish() called candidate.finalize() — the engine's Approved→PublicationReady gate, an irreversible in-memory transition (candidate.rs:431) — before validating repository/commit-sha/semantic-version. The first bad-input publish consumed the transition; the second call then failed at the state gate ("teaching compiler error: invalid state: current publication-ready, requires approved") before identity validation ever ran — exactly the reproduced symptom. Fix: identity-input validation now runs before finalization: invalid inputs report the input error ("invalid repository identity…", "invalid commit sha…", "invalid semantic version…") and the candidate stays Approved for a corrected retry; publish_requires_an_approved_candidate/rejection_leaves_the_candidate_unapproved still surface finalize's "requires approved" because their inputs are valid.

Startup-sweep visibility for a seeded orphaned instance (cancel_is_explicit_and_terminal, panic at workflow_tests.rs:414): the frozen DurableInstanceStore::open loads its snapshot file exactly once into an in-memory map; records() never re-reads. The plane had opened/cached its DurableStores handle during publish() (empty at that moment), so the test's post-open seed of an orphaned Running instance through a second handle was invisible: instance_list → empty → index out of bounds: the len is 0 but the index is 0 (reproduced verbatim). Fix: the read path (instance_list, instance_get) now calls refresh_stores() — re-open the durable root (documented supported operation: snapshots load atomically, journals replay in order; every prior mutation was flushed atomically, so the swap loses nothing) and swap the cached handle — then runs the one-shot startup sweep over that refreshed view, yielding the documented restart semantics (orphaned Running → Paused with persisted position, never silently Running, never auto-executing) without reconstructing the plane. Mutating commands keep the single-writer cached-handle discipline.

verification commands and exact results (all REAL runs, rustc 1.95.0 via the workspace's rust-toolchain.toml — the Tech Lead's exact compiler; baseline reproduced the three failures with matching signatures before any edit):

cargo test -p codex-app-server --lib workflow::tests -- --test-threads=1 → test result: ok. 11 passed; 0 failed; 0 ignored; 0 measured; 305 filtered out

=== END RWO-001-FIX COMPLETION REPORT ===

(The worker's full run also included the complete lib + protocol suites per the work order; the DOM-extracted report tail was truncated by the transcript's "Show full message" collapse — the Tech-Lead independent run below covers every suite.)

## Tech-Lead Independent Verification (2026-09-13 18:15–18:51 UTC, post-sandbox-reset rebuild)

- Environment: rustc 1.95.0 (59807616e 2026-04-14), cargo 1.95.0, -j1, CARGO_PROFILE_DEV_DEBUG=0, CARGO_INCREMENTAL=0, RUST_MIN_STACK=33554432; /tmp target (memory-disciplined; the protocol test-binary link OOM-kills with the replay stack up — verified twice — the full-memory window gives 3.6G).
- Bundle harvested from the live worker pod (ws-e0c9b9b7) via the workspaces files API: 4114 bytes, `git bundle verify` OK, requires d003b025e.
- Gates: exactly 1 commit over base; head 772baceb1 matches the report; diff scope = codex-rs/app-server/src/workflow.rs only (+110/−17); workflow_tests.rs byte-identical; no frozen-crate edits (git diff --name-only).
- Code review: reconciled_hybrid_session before compile (observed-before-instructed through the frozen crate's public API, per-group order preserved); identity-input validation moved before the finalize state gate (candidate stays Approved on bad input); refresh_stores + lazy startup sweep on the read paths (single-writer discipline kept for mutations). APPROVED.
- Test results (real runs):
  - `cargo check -p codex-app-server-protocol -p codex-app-server` → CLEAN (workspace [lints] inject clippy denies into plain rustc → check == clippy-clean).
  - `cargo test -p codex-app-server-protocol` → **309 passed / 0 failed / 1 ignored**.
  - `cargo test -p codex-app-server --lib` → **316 passed / 0 failed** (313 baseline + the 3 fixed; zero regressions).
  - workflow suite detail → **11/11**, including teach_compile_review_approve_publish_in_all_three_modes, publish_validates_the_identity_inputs, cancel_is_explicit_and_terminal.
- Merged-main re-verification: `git diff 772baceb1..main --name-only` = docs/evidence/fixtures only — **zero .rs files differ** from the verified tree (RWO-003/#33 + RWO-004/#34 changes are fixture/doc-layer); the Rust verification transfers to merged main df8261b23c by identity.
- Verdict: **Family A (P0 — no workflow/teaching control plane behind user-facing surfaces) CLOSED**: the mount is live behind protocol + app-server + CLI surfaces with the full teaching-mode contract enforced (reconciled hybrid origins, validated publish identity, crash-safe instance semantics).

## Deviations / Notes

- The RWO-001 mount originally shipped with 3 deterministic service-test failures (routed back to the same worker as RWO-001-FIX per the remediation loop's role boundary — the Tech Lead does not implement); the integration-fix commit d003b025e (compile corrections + fixture typo + event_id serde + real-generator .zst) was applied by the Tech Lead as verified, non-semantic corrections.
- The 2026-09-13 sandbox reset destroyed the local verification target mid-pipeline; the toolchain was rebuilt (rustup 1.95.0) and the ENTIRE verification re-run from scratch on the recovered bundle — the results above are post-reset, single-source-of-truth.
- CI: the repo's Actions have zero historical runs (verified via the API — RWO-003/#33 and RWO-004/#34 also merged on local verification); the merge gate is the Tech-Lead real-toolchain run above.
