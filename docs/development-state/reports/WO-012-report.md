=== WO-012 COMPLETION REPORT ===

Work Order ID: WO-012
base branch + base SHA: main @ a5c2b85487d23c77d7d2ca13d1e092b50ef1cbd9 (dispatch base, verified reachable with git rev-parse a5c2b85487d23c77d7d2ca13d1e092b50ef1cbd9^{commit}; fallback not needed. Note: origin/main HEAD had advanced to f5a77a6d5cd0ddae0f67a4729879e2bc3872d631 post-dispatch — WO-014 merges — but the dispatch base was reachable, so I based on it exactly as instructed)
head SHA (your branch, after your commit): 5ac1f50bd2bd72a32f67bc8e0049995cff853824 on branch wo-012/workflow-distribution
changed files/surfaces:
NEW crate codex-rs/workflow-distribution/ (lib codex_workflow_distribution, 14 files): src/{lib,error,state,metadata,commerce,entitlement,access,port,discovery,memory}.rs + src/{state,metadata}_tests.rs + tests/e2e_workflow_distribution.rs + Cargo.toml
codex-rs/Cargo.toml: exactly ONE members line ("workflow-distribution")
codex-rs/Cargo.lock: one new [[package]] entry (14 lines) — same pattern as the WO-011 crate-adding commit 9bd959d3b
NO existing crate edited (workflow-contracts/forge/triggers/app and below are untouched, as required); 16 files changed, 4658 insertions
implementation summary:
New leaf crate = the publication/distribution/licensing/marketplace plane. Runtime deps: codex-workflow-contracts + codex-workflow-forge (publish/install/discovery semantics composed directly); dev-deps: codex-workflow-app + codex-workflow-triggers for the E2E composition proof. The plane sits over the WO-011 installation/configuration model at the host orchestration level (marketplace install returns the sealed WorkflowVersion the host feeds to codex_workflow_triggers::InstallRequest; both planes share the forge install-registry discipline — the distribution install deliberately does not touch the forge registry). The AccessPolicy port deliberately reuses the WO-011 ResourceAuthorizer/ResourceAuthorization shape (decision-as-data {allowed/authorized, source, reason}, denial never a credential, in-memory explicit-allow/default-deny double) with the reference documented in module docs; authorization semantics are referenced, not duplicated (the live clone was authoritative; the source bundle matched it exactly — no divergence found).
Distribution states: DistributionState {Private, Shared, Public, Forked, Installed, Published} over immutable versions. Published = unlisted (secret-link: addressable by identity, invisible to discovery — the plugin-catalog listed/unlisted/private pattern from the capability map); Forked = derived release, owner-visible until explicitly shared/listed; Installed = consumer pin. Transitions (Share{audience}, MakePublic, MakePrivate) go through the explicit port method apply_transition with a validated legal graph; no-ops refused; Installed records never transition; a published version is sealed forever and no transition touches its meaning.
Publication metadata: PublicationMetadata — versioned (PUBLICATION_METADATA_FORMAT_VERSION) and hash-chained to the release seal (ContentDigest over the PublishedVersionRef + all covered fields): attribution, ownership (MarketplacePrincipal, credential-free by construction), licensing (LicenseTerms: SPDX-style identifier + optional URL + optional custom-terms digest), provenance (SourceLineage: version-level fork lineage + carried attribution), compatibility (CompatibilitySpec: minimum runtime + capabilities + taxonomy classes + resources, validated to COVER the version's own declared step capabilities/resources), upgrade policy (UpgradePolicySetting {Pin, Follow} with documented mapping to/from the frozen contract UpgradePolicy).
DistributionPort/MarketplacePort seam with InMemoryMarketplace reference implementation: publish (re-verifies version integrity, metadata hash chain, compat coverage, commercial terms binding; refuses duplicates), search (facets + visibility scopes), fetch_version/fetch_listing (by workflow + immutable version id, re-verifying), fork (re-seals the upstream's frozen definition + dependency lock in the fork's repository/revision/version with sealed fork lineage AND metadata lineage pointing back; upstream never mutated; same-repo and unattributed forks refused), install (gate order visibility → integrity → already-installed → access policy → commercial entitlement → record; a denial records nothing), apply_transition, evaluate_upgrade, decide_upgrade, installed.
Commercial entitlements as a separate boundary: CommercialTerms (digest-bound to the release; CommercialPolicy {Free, PaidRelease, Subscription, LicenseGrant} — policy class only, deliberately no prices/settlement/credentials) behind EntitlementPort performing checks only; grants live in a host-owned store (the in-memory registry is seeded by the host). Denial gates distribution/installation only.
Upgrade policy: Pin never surfaces proposals; Follow surfaces pure-data UpgradeProposals that advance only through an explicit UpgradeDecision (rejection recorded with pin kept; approval re-runs every install gate for the new release — a gate-refused upgrade is refused with decision data and changes nothing; stale proposals refused), mirroring the forge's reviewable plan/decision/audit shape.
tests/commands and exact results (real toolchain — rustup stable installed this session: cargo 1.98.1 / rustc 1.98.1):
cargo test -p codex-workflow-distribution → 10 unit + 8 E2E = 18 passed, 0 failed ("test result: ok. 10 passed; 0 failed" and "test result: ok. 8 passed; 0 failed")
cargo clippy -p codex-workflow-distribution --all-targets -- -D warnings → clean ("Finished dev profile … no warnings/errors")
cargo fmt -p codex-workflow-distribution -- --check → exit 0 (warnings about the repo's nightly-only imports_granularity rustfmt.toml option on stable are pre-existing repo-wide behavior, not failures)
One transient failure during development was a linker bus error caused by sandbox disk exhaustion (9.3G/9.9G) — resolved by reclaiming the incremental cache; not a code issue.
acceptance-criteria evidence (map each bullet to code + tests):
Author can publish an immutable workflow release: publish re-verifies WorkflowVersion::verify_integrity + metadata hash chain before recording; republish refused (AlreadyPublished) — E2E publishing_seals_releases_and_rejects_every_tampered_record (tampered version → VersionIntegrity, tampered metadata → hash-chain failure, misbound/tampered commercial terms → refused, understated compatibility → refused, scope transitions leave the version byte-identical, no-op transitions refused) + unit tests metadata_tests (tamper breaks chain; tampered release refs never seal; digest determinism).
Support private, shared, public, forked, installed, published states: state.rs six-variant enum + validate_transition graph + PublicationScope — unit tests state_tests (all legal transitions, refused no-ops/installed/empty-audience) + E2E state assertions throughout (Private/Shared/Public/Published/Forked/Installed all reached and asserted).
Attribution, ownership, licensing, provenance, compatibility, upgrade policy metadata: PublicationMetadata fields; fork metadata carries lineage + carried attribution; fork sealed provenance carries repository-level ForkLineage + authors + license + upgrade policy — E2E forking_derives_new_immutable_versions_with_lineage_provenance asserts new version id, identical definition digest, both lineage levels, upstream untouched, provenance search facet.
Distribute/install under an access or license policy: AccessPolicy port + gate order; E2E installs_are_license_gated_with_deny_and_allow_paths (allow path pins the immutable version and carries the decision; default-deny refuses with decision data and records nothing; re-install refused; private/fork invisible to others; unlisted addressable-but-undiscoverable).
Commercial entitlements without modifying executable semantics or exposing credentials: E2E entitlement_denial_gates_installation_only_and_never_mutates_semantics (denial → InstallRefused with entitlement decision data, record byte-identical before/after; grant → install succeeds, record still byte-identical; unentitled upgrade approval → UpgradeRefused, pin unchanged, history empty).
Installed/executed workflow identity remains bound to an immutable release: every install/upgrade pins PublishedVersionRef + re-verifies verify_integrity; E2E marketplace_installs_hand_off_to_the_trigger_plane_with_the_same_identity proves the full hand-off (marketplace install → InstallRequest::new(version) → WorkflowTriggerPlane::install → configuration + forge InstallRegistry pin the same WorkflowVersionId; old pinned versions stay valid fetchable records after upgrades).
No credentials in workflow source or ordinary evidence: no-credentials invariants are structural — E2E no_credentials_appear_in_any_distribution_record (repository identity canonicalizes userinfo away; principals reject credential shapes; serialized listings/installs/proposals/requests/decisions contain no credential-shaped keys or URL-userinfo values).
compatibility impact (especially: ordinary Codex behavior untouched when no workflow is active): zero existing code changed (one workspace members line + one lock entry, additive). The crate is a leaf; nothing depends on it. With no marketplace/publication/installation, every operation is an inert UnknownRelease/NotInstalled result — no adapter probed, no version selected, no instance created, no codex-core involvement. The workspace-level warmup cargo check -p codex-workflow-triggers (whole dependency stack) still compiles clean with the new member added.
known limitations:
InMemoryMarketplace is the only DistributionPort implementation (reference semantics/test double by design; host marketplace transport is out of scope).
Install records are one pin per workflow (tenant scope), mirroring the forge InstallRegistry — multi-tenant marketplaces are a host-deployment concern.
evaluate_upgrade picks the newest other published release by semantic version; richer upgrade channels/ranges are future host surface.
UpgradePolicySetting::from_contract maps AutomaticWithinRange → Follow (documented one-way lossiness: automatic application is host approval behavior, not a distribution distinction).
deferred items + owning WO: payment processing, pricing, settlement, billing, and marketplace service topology (intentionally unfrozen by CODEX-UNIVERSAL-LOCK — host/marketplace-owned, no WO in this repo); durable persistence of distribution records (workflow control plane / roadmap M4 continuation); real forge-backed marketplace transport (host application integration); publishing to MarketplaceListing UI surfaces (client WOs); just bazel-lock-update/MODULE.bazel.lock refresh for Bazel builds (not run — matches the WO-011/WO-013/WO-014 crate-adding precedent, which also did not touch Bazel locks; the Tech Lead's independent verification compiles via cargo).
risks:
Rustfmt on stable cannot apply the repo's nightly-only imports_granularity=Item rustfmt.toml option (pre-existing, repo-wide; --check still exits 0, but nightly fmt could reorder my import lines slightly on the Tech Lead's machine).
The AccessPolicy-shape reference to WO-011 (rather than a hard runtime dependency on codex-workflow-triggers) is a documented interpretation of "compose or reference them" — the E2E composition test (dev-dep) makes the layering real and verified, but the Tech Lead should confirm the reading.
Published (unlisted) releases being installable-by-identity by any principal (before the access-policy gate) is a deliberate secret-link semantic; a stricter host implementation can tighten it behind its own AccessPolicy.

Artifacts: branch wo-012/workflow-distribution @ 5ac1f50bd in /home/z/codex (all files kept in the working tree for harvest); delivery bundle /home/z/my-project/wo-012-delivery.bundle (45,404 bytes, git bundle verify OK, contains the branch head, requires base a5c2b854); work log at /home/z/my-project/worklog.md. Not pushed — the Tech Lead applies, independently verifies, pushes, and merges.

