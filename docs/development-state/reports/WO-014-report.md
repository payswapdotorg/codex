=== WO-014 COMPLETION REPORT ===

Work Order ID: WO-014
base branch + base SHA: main @ a5c2b85487d23c77d7d2ca13d1e092b50ef1cbd9 (verified: git rev-parse a5c2b85487d23c77d7d2ca13d1e092b50ef1cbd9^{commit} resolves and equals origin/main HEAD at dispatch; the network clone succeeded and was authoritative — the fallback source bundle matched the live tree wherever compared, no disagreements to report)
head SHA (your branch, after your commit): fb6036e1f155455224ed76771099573664d7d784 (branch wo-014/workflow-evolution, commit message feat(workflow-evolution): WO-014 learning and governed evolution; delivery bundle at /home/z/my-project/wo-014-delivery.bundle, verified to contain exactly this commit on top of the base)
changed files/surfaces: 18 files, 5205 insertions, 0 deletions — new leaf crate codex-rs/workflow-evolution/ (Cargo.toml + 14 src files + 1 E2E test file, lib codex_workflow_evolution) plus exactly one members line in codex-rs/Cargo.toml ("workflow-evolution") and the corresponding codex-rs/Cargo.lock entry. No existing crate was edited; no other file touched.
implementation summary:
Candidate contract (candidate.rs): ImprovementCandidate = proposed change of five kinds (DefinitionDelta, CapabilityBinding, RecoveryPolicy, DependencyChoice, ScheduleTuning) + full provenance (EvidenceReference list + content-addressed RunProvenance fingerprints); content-addressed CandidateId; applied_over applies a change read-only over the incumbent to produce successor definition+lock (non-step/missing binding targets and identity-changing deltas refused); VersionBump derives plain successor semvers.
Evidence-driven generator (generate.rs): CandidateGenerator is pure over a read-only EvidenceCorpus (verified incumbent version + run records + evidence references + observed environments/schedule). Five deterministic rules map evidence streams to the five change kinds; no live engine state and no model/provider types in the API (model suggestions cannot enter as authorization by construction).
Validation pipeline as ports (validate.rs): generic ReplayPort (in-memory EvalReplayPort replays incumbent vs sealed candidate through eval-compat's WorkflowEvalHarness with cloned scripted model/env doubles), the fixed differential gate (eval-compat compare_workflow_runs; any enumerated divergence fails), and PolicyPort (PolicyGate over EvolutionPolicy: authorization per change kind, resource check refusing undeclared-capability introduction, compatibility check confining successors to allowed bumps). Each stage records StageEvidence; ValidationReport::passed() requires all three stages ran and passed explicitly.
Governed evolution (approval.rs + evolution.rs): ApprovalPort/InMemoryApprovalPort is the only authorization path — explicit ApprovalDecision by a host principal, bound to candidate id + incumbent + validation digest. EvolutionGovernor workflow: record (scrub + retention gates) → validate (mint immutable forge revision, seal successor at that revision, run the pipeline; re-validation mints a fresh identity so stale approvals die) → approve → publish_successor (forge publish_release at the staged revision + append-only VersionLineage carrying predecessor/successor refs, candidate provenance, validation digest + stage summaries, approval, release tag, and the optional AppliedAdjustment) → upgrade_installation / rollback_to_predecessor (both explicit propose+decide transitions through the forge InstallRegistry, recorded in its history). The predecessor is never mutated: every read re-verifies integrity, publication only adds records, and no-op evolutions are refused. Recovery-policy/schedule candidates publish a governed re-release of incumbent content with the adjustment riding lineage, so effective_adjustment() derives from the installed version and version rollback uniformly rolls adjustments back.
Retention + credential invariant (retention in ledger.rs, scrub.rs): RetentionPolicy bounds candidates (FIFO eviction on every insert) and evidence provenance per candidate (loud ProvenanceTooLarge refusal); scrub_json/require_clean_json/require_clean_text detect secret-bearing JSON keys and credential value shapes (PEM, AWS keys, sk- keys, GitHub/Slack tokens, URL-embedded credentials — matched per whitespace token so hyphenated words don't false-positive) and refuse them at candidate recording, approval notes/reasons/approver, and upgrade/rollback notes; findings record path+reason only, never the matched content.
tests/commands and exact results (real toolchain — Rust 1.95.0 installed matching the repo's rust-toolchain.toml pin; cargo 1.95.0 (f2d3ce0bd 2026-03-21)):
CARGO_PROFILE_DEV_DEBUG=0 CARGO_PROFILE_TEST_DEBUG=0 CARGO_INCREMENTAL=0 cargo test -p codex-workflow-evolution → test result: ok. 25 passed; 0 failed; 0 ignored (lib unit tests) and test result: ok. 10 passed; 0 failed; 0 ignored (tests/evolution_e2e.rs) = 35/35 passed.
CARGO_PROFILE_DEV_DEBUG=0 CARGO_INCREMENTAL=0 cargo clippy -p codex-workflow-evolution --all-targets -- -D warnings → exit code 0 (zero warnings under the workspace lint set, incl. expect_used/unwrap_used deny outside tests).
cargo fmt -p codex-workflow-evolution -- --check → exit code 0 (the emitted imports_granularity "unstable feature" warning is the repo's pre-existing rustfmt.toml behavior on stable toolchains; check passes).
Profile env overrides were used only because the 9.9 GB sandbox rootfs cannot hold a full-debuginfo build of the dependency closure (a default-profile attempt failed with ENOSPC); the test binaries are identical modulo debug info. just/cargo-nextest are not installed in the sandbox, so the scoped cargo commands from the Work Order instruction were run directly.
acceptance-criteria evidence (map each bullet to code + tests):
"Generate improvement candidates for workflow definitions, capability bindings, recovery policies, dependency choices, and scheduling." — generate.rs five rules; unit tests generate_tests.rs (each rule fires from its stream and only from it); E2E evidence_to_candidate_provenance_is_traceable, recovery_adjustments_ride_the_lineage, dependency_choices_minimize_excess_locks.
"Preserve provenance from evidence to candidate change." — CandidateProvenance/RunProvenance; E2E asserts generated candidate provenance == the real run's evidence references + fingerprint, and lineage carries it unchanged (assert_eq!(publication.lineage.provenance, binding.provenance)).
"Validate candidates through replay, simulation, evaluation, and policy checks." — ValidationPipeline replay+differential+policy with per-stage evidence; E2E replay_divergence_fails_the_gate (a behavior-changing drop-step candidate fails with enumerated divergences; publication refused), policy_gates_refuse_unauthorized_and_incompatible_changes (capability escalation + major-bump refusal). Replay uses the teaching compiler's frozen walk policy inside the WO-010 lifecycle via eval-compat (simulation/evaluation surface of the merged planes).
"Require explicit approval/publication to create a new immutable workflow version." — approval.rs port-only authorization + digest binding; E2E approval_is_required_and_rejection_is_honored (no approval → ApprovalRequired; rejection → ApprovalRejected and nothing published; only explicit approval publishes; upgrade before promotion → CandidateNotPromoted).
"Preserve rollback and lineage from predecessor to successor versions." — VersionLineage + rollback_to_predecessor via the reviewable install-registry path; E2E lineage_and_rollback_transitions_are_recorded (full history: upgrade applied, rollback rejected, rollback applied; lineage stage summaries all passed; NoPredecessorToRollback when exhausted) and recovery_adjustments_ride_the_lineage (adjustment effective on install, removed by rollback).
"Execution evidence can produce traceable improvement candidates that are evaluated and, when approved, published as new immutable versions with complete lineage; existing versions remain unchanged." — the golden path across the E2E tests; the_predecessor_is_immutable additionally proves the predecessor record byte-identical + still verifying after full evolution, tampered incumbents refused at governor construction (contract VersionIntegrity), and no-op evolutions refused (NoopEvolution).
Forbidden list: silent online mutation (no write path to published records; integrity re-verified per operation), model suggestions as authorization (no model type in the learning/approval APIs; approval arrives only via explicit ApprovalDecision), unbounded credential retention (scrub gates + retention caps; E2E credentials_never_enter_candidates_or_approvals, retention_bounds_the_ledger, unit ledger_tests.rs/scrub_tests.rs), learning bypassing evaluation/control-plane authority (promotion requires the three-stage report + matching approval; generator output is never authority).
compatibility impact: none on ordinary Codex behavior — the change adds one leaf crate and one workspace-members line; no existing crate, binary, CLI, config, or protocol surface was touched. The crate mounts nothing into codex-core; like the other workflow planes, nothing executes unless the host explicitly drives the evolution lifecycle over an installed workflow version.
known limitations:
The governor holds in-memory state (forge simulation, releases, install registry, known version records, ledger, lineage); durable implementations are host-owned (documented in the crate and module docs, mirroring the InstallRegistry/workflow-app precedents).
Replay rides eval-compat's scripted model/environment doubles; live-provider differential evaluation needs an external-effects basis owned by a later Work Order.
EvolutionPolicy's capability-introduction flag, bump ceiling, and allowed-change set are data, not RBAC; workflow-role-based approval authority belongs to the control plane.
Credential detection is shape-based (bounded, documented patterns), not cryptographic — a determinedly obfuscated secret is out of scope of a static guard.
deferred items + owning WO: durable/control-plane integration of the ports (approval plane wiring to native Codex approvals, evidence plane to rollout traces, install/lineage persistence) — the Codex application / roadmap M4 control plane; live-provider replay benchmarks — a later evaluation WO with an explicit external-effects basis; richer scheduling calendars (cron/time zones) — the trigger-plane follow-up already noted in workflow-triggers; marketplace/monetization surfaces — WO-012 (dispatched separately).
risks: the differential gate enforces observable-equivalence promotion by design (behavior-changing improvements are generated and validated but deliberately fail this gate — they must route through the normal teaching/compiler authoring path as new candidates, which is the frozen architecture's intent, but product may perceive it as conservative); approval digest binding means any re-validation invalidates prior approvals (correct governance, but hosts must re-decide after re-validation); the in-memory ledger evicts candidates FIFO under the retention cap (lineage/registry keep the immutable audit trail, but candidate detail is pruned — documented); the sandbox could not run a default-debuginfo build (ENOSPC), so verification ran with debug info disabled — semantically identical test binaries, and the Tech Lead's independent compile will reproduce the results under the standard profile.

The working tree at /home/z/codex (branch wo-014/workflow-evolution, all files present, cargo target cleaned) and the bundle /home/z/my-project/wo-014-delivery.bundle are ready for harvest. No push was performed.

