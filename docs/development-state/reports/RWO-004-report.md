# RWO-004 Completion Report (filed by Tech Lead)

**Work Order:** RWO-004 — PressRoom publication terminality + publish version guard (VWO-010 Family F)
**Session:** chat.z.ai agents-tab, GLM-5.3 + Full-Stack, chat 407069df-e8d8-40a6-8504-4287f84c306a
**Dispatched:** 2026-09-13 00:24 UTC · **Completed:** 2026-09-13 00:52 UTC (clean run, zero stall-recovery cycles)
**Delivery:** git bundle RWO-004-delivery.bundle (head 545a0ff6f6bbb45e16865abc3300ee7673a5a797, exactly 1 commit over dispatch base 2d37cc0ef)

## Worker's Completion Report (verbatim)

=== RWO-004 COMPLETION REPORT ===

Work Order ID: RWO-004
base branch + base SHA: main @ 2d37cc0ef7d075e877f370790f82ac9c70df26a4 (verified: git rev-parse 2d37cc0ef7d075e877f370790f82ac9c70df26a4^{commit} → same; origin/main has since moved to 45c21602 via the RWO-003 merge, but the dispatch base was reachable and used exactly)
head SHA (your branch, after your commit): 545a0ff6f6bbb45e16865abc3300ee7673a5a797 (branch rwo-004/pressroom-publication-terminality, single commit, parent = base; bundle at /home/z/my-project/RWO-004-delivery.bundle verified "okay", requires base ref, contains exactly this commit)
changed files/surfaces:
docs/validation/fixtures/media/ops.js (the RWO's fix scope) — editStory status gate + publishStory version guard + header/inline comments
docs/validation/fixtures/media/ui.js — publish form passes the version it approved (RWO fix scope item 2) + form note
docs/validation/fixtures/verify-sweep.sh — the five media publish bodies now pass the story's current expectedVersion (body-only + one explanatory comment)
docs/validation/evidence/rwo-004/ — 4 new evidence artifacts (post-hoc: familyF-before-after-reproduction, acceptance-criteria, verify-sweep; _infra: environment-and-base-identity)
No engine crates, no RWO doc / six validation reports / REPORT-SCHEMA / SCENARIO-CATALOG / FAILURES.md changes (verified byte-identical: git diff --name-only HEAD -- docs/validation/work-orders/ docs/validation/reports/ → empty).
implementation summary:
editStory: immediately after the author-or-editor permission check, a status gate — EDITABLE_STATUSES = ['draft','changes_requested','in_review','approved']; anything else (above all published/corrected) throws HTTP 409 terminal_state with the duplicate-publish-style wording "… use a correction instead." (extra: storyId, status, version). No mutation, no story.edited event, no version bump on rejection.
publishStory: after the story_not_approved check, expectedVersion is REQUIRED — absent → HTTP 400 expected_version_required (names the current version, mentions reload-and-re-review); then the EXISTING checkVersion(ctx, story) helper is called → stale version → 409 data_conflict naming both versions ("reload and reapply"). Guard sits before the hero/missing_asset and channel checks.
ui.js publish form gains W.hidden('expectedVersion', story.version) (+ note), so the form/UI twin passes the approved version, exactly as the RWO's fix scope item 2 mandates; the UI edit-form guard for published stories is untouched ("UI-only guards stay; the API twin must now match them").
Sweep alignment: the five media publish bodies in verify-sweep.sh pass the current version (golden st-0206 v1; seed st-0205 v1; seed st-0204 v2) — forced by the contract itself (criterion 2 mandates 400-on-absent while verification command 2 mandates 59/59; at base the sweep's publish callers carried no version). Body-only change; expected codes and check count unchanged (59 before and after).
verification commands and exact results:
bash docs/validation/fixtures/run-all.sh --reset → "All five VWO-002 fixture apps are running", all five health checks healthy
bash docs/validation/fixtures/verify-sweep.sh → RESULT: 59 passed, 0 failed (run at base AND on the branch; the final run repeated the exact command pair after the change)
VWO-010 Family F attack 1 re-run (identical request at base and branch): POST /api/stories/edit (hana.kim, author) on published st-0206 {body:'MUTATED AFTER PUBLICATION - terminality breach (RWO-004 probe)', expectedVersion:2} → base: HTTP 200 "Story st-0206 saved as v3.", body mutated, story.edited emitted; branch: HTTP 409 terminal_state "Story st-0206 is already published — its body is closed for editing; use a correction instead."; story state unchanged (body "Probe A body before publication.", version 2), zero story.edited events for the probe.
Attack 2 re-run (version guards): publish without expectedVersion (post-approval sneaked edit at v2) → base: HTTP 200, sneaked edit went LIVE; branch: HTTP 400 expected_version_required ("currently v2"; story stays approved v2). Publish with stale expectedVersion:1 → base: HTTP 200 (no check existed); branch: HTTP 409 data_conflict "you worked from v1 but the current version is v2 — reload and reapply." Publish with current expectedVersion:2 → HTTP 200 published (happy path unchanged).
Normal editorial path re-run: create → attach → submit → approve → publish {expectedVersion:1} (200, published v2) → correct {expectedVersion:2} (200, "Correction c-800802 applied; story re-distributed to 1 channel(s).", corrected v3, body replaced "meets at 19:00"→"meets at 20:00") → ok; correction path unchanged (op untouched by the diff).
Extra oracles: editor (camille) edit on published seed st-0201 → 409 terminal_state (base: 200 mutation — gate is role-agnostic); author edit on the corrected story → 409 terminal_state (base: 200 mutation); UI edit form still ABSENT on the published story page; UI publish form on approved st-0204 now renders <input type="hidden" name="expectedVersion" value="2"> (absent at base).
acceptance-criteria evidence (map each RWO acceptance criterion to its verification transcript):
Author edit on published/corrected via API twin → 409 terminal_state, no story.edited event, version unchanged → docs/validation/evidence/rwo-004/post-hoc/20260913T003903Z-acceptance-criteria.txt §Criterion 1, with raw transcripts in 20260913T003903Z-familyF-before-after-reproduction.txt (BEFORE section 1: 200 "saved as v3" + event; AFTER section 1: 409 terminal_state, GET oracle showing body/version unchanged, empty story.edited feed, UI form scan; corrected-half + editor probes in sections 3–4).
Publish without expectedVersion → 400 expected_version_required → acceptance-criteria.txt §Criterion 2 + reproduction transcript AFTER section 2 (B1: 400 with currentVersion: 2, story stays approved; BEFORE: 200, sneaked edit live).
Stale expectedVersion → 409 data_conflict ("reload and re-review"); current version → ok → acceptance-criteria.txt §Criterion 3 + reproduction transcript AFTER section 2 (B2: 409 data_conflict v1-vs-v2; B3: 200 published; BEFORE: both 200, no guard).
Correction path unchanged → acceptance-criteria.txt §Criterion 4 + reproduction transcript section 3 (AFTER and BEFORE both 200/200 through publish→correct; applyCorrection untouched, git diff --stat in _infra/20260913T003903Z-environment-and-base-identity.txt).
Sweep gate → post-hoc/20260913T003903Z-verify-sweep.txt (base 59/59 + branch 59/59 full transcripts).
Environment/base identity → _infra/20260913T003903Z-environment-and-base-identity.txt (base SHA rev-parse output, node v24.19.0 / bash 5.2.37 / curl 8.14.1 / uname, tree state).
rollback note: revert commit 545a0ff6f (single commit) — fixture/UI/sweep bodies return to base behavior; published stories keep their history; seeds reset on run-all.sh --reset; no state migration. Per the RWO's rollback section this is a fixture-layer change only (+ the RWO's own note that catalog wording rides RWO-010).
known limitations / honest deviations:
changes_requested included in EDITABLE_STATUSES (RWO text lists "draft/in_review/approved"). Rationale: the seeded UI revision loop (request-changes → author revises → resubmit; ui.js canEdit offers edit for draft/changes_requested/in_review, documented in VWO-007) would break — the UI edit form would render but the op would 409 — with zero security benefit, since changes_requested is pre-publication; the dispatch prompt itself says "the API twin must now match them [the UI guards]". The security boundary (published/corrected → 409 terminal_state) is exactly as specified and is what acceptance criterion 1 tests. Flagging for the Tech Lead in case the literal 3-status allowlist was intentional.
verify-sweep.sh media publish bodies updated (5 calls gain the current expectedVersion; + one comment). Not named in the "Fix scope (bounded)" file list, but logically forced: the contract simultaneously mandates 400-on-absent (acceptance criterion 2, re-verified in the attack re-run) and 59/59 (verification command 2) — at base the sweep's publish callers pass no version, so a strict gate turns 3–4 sweep checks red (e.g. the golden publish would 400). The rollback section's "single-file fixture change" is already inaccurate w.r.t. the RWO's own ui.js requirement ("Editor UI/publish form must pass the version it approved"); I treated the sweep's publish callers as the same class of required caller alignment. Body-only diff; expected codes/counts unchanged (59 before and after).
409 wording nuance: criterion 3 glosses "reload and re-review"; the existing shared checkVersion helper (which the RWO explicitly instructs to call) emits its canonical "reload and reapply" message (the documented recovery phrase across VWO-007/008 evidence, shared with edit/correct). I left the helper byte-identical per the bounded-scope rule; the "re-review" phrasing appears in the new 400 message and the UI form note.
No cargo toolchain in this sandbox — irrelevant to scope (no engine crates touched; fixture work ran for real on node v24.19.0). Rust-side static verification of "no frozen-crate edits" is trivially git diff --name-only (empty outside docs/validation).
UI oracle re-verified by server-rendered HTML form scan (curl) rather than agent-browser — the story pages are server-rendered, so the form-action/hidden-input scan is equivalent evidence; noted in the transcripts.
Open question (not a deviation): RWO-010's catalog wording alignment for the publish step (expectedVersion parameter) remains pending and complements this change — intentionally left untouched here.
=== END RWO-004 COMPLETION REPORT ===
## Tech-Lead Independent Verification (2026-09-13 01:0x UTC)

- Bundle harvested via workspaces API (16,628 bytes; git bundle verify OK against base 2d37cc0ef).
- Gates: 1 commit over base; head 545a0ff6 matches report; diff scope = media/ops.js (+28/-7), media/ui.js (+3/-1), verify-sweep.sh (+13/-1) + 4 evidence files only.
- Code review: editStory status gate (EDITABLE_STATUSES; terminal -> 409 terminal_state, correction wording); publishStory expectedVersion required (400) + checkVersion (409 data_conflict); UI publish form ships current version; sweep publishes updated. APPROVED.
- Independent verification (node v24.19.0): run-all 5/5 apps; verify-sweep **59/59 passed**; Family F live probes — edit published st-0201 -> **409 terminal_state**; publish st-0205 without expectedVersion -> **400 expected_version_required**; stale v99 -> **409 data_conflict**; correct version -> natural 404 missing_asset (no hero), golden publish 200 in sweep.
- Post-merge: PR #34 squash-merged (95e26195f); verify-sweep on merged main (RWO-003 + RWO-004) -> **59/59 passed**.
- Verdict: Family F (P0 publication terminality / unreviewed-publish) CLOSED on the PressRoom fixture.
