# RWO-010 — Harness Alignment Batch: Catalog/Seed/Fixture Drift (VWO-004/005 P2/P3 Items)

**Status:** PROPOSED (VWO-010 synthesis)
**Priority:** P2/P3 batch (Drift family)
**Closes:** VWO-004 F2, F3, F4, F5, F6; VWO-005 F7. (VWO-006 F-5 is NOT here —
it rides RWO-015.)
**Depends on:** none (independent of all engine work)

## Objective

Align the scenario catalog, the seed data, and the fixture behaviors the six
reports flagged as harness-internal drift, so the next revalidation wave runs
the catalogued paths as written.

## Findings closed (all harness-internal; fixtures behaved fail-closed throughout)

- VWO-004 F2 (P2): catalog step 4 of construction-procurement-approval expects
  a second OPEN purchase request with a valid vendor — the shipped seed has
  none. FIX: add `prq-5002` (OPEN, valid vendor, distinct amount) to the
  construction seed (preferred — keeps the catalogued approve→PO sub-path
  exercisable).
- VWO-004 F3 (P2): catalog step 4 of rideshare-support-escalation expects the
  support agent to resolve; the seed grants `ticket:resolve` to ops only. FIX:
  grant `ticket:resolve` to `ana.silva` (support_agent) in the rideshare seed
  (preferred) OR rebind the catalog step to the ops role.
- VWO-004 F4 (P2): catalog lists `missing_asset` on the rendition op; PressRoom
  implements `data_conflict` on rendition (missing_asset is bound to
  attach/publish). FIX: implement the `missing_asset` branch on
  `requestRendition` (unresolvable asset → 404 missing_asset) — preferred
  (user-meaningful catalogued failure) — or rebind the catalog switch.
- VWO-004 F6 (P2): catalog fast channels are "web+push"; PressRoom has no push
  channel. FIX: add a `push` channel kind to the media seed (fast, licensed
  differently from partner-app) OR rebind the catalog wording to
  web-front-page + rss-feed.
- VWO-004 F5 (P3): follow-up-action note surface is realized as
  close-with-resolution-note. FIX: catalog wording alignment (accept the
  realization, rename the step).
- VWO-005 F7 (P3): approved-story attach dead-end — a hero cannot be attached
  while a story is approved (only pre-approval). FIX: allow hero re-attach
  while `approved` (returns to review? NO — smallest: allow attach, publish
  gate re-validates) OR annotate the catalog path that hero must be attached
  before submission.

## Reproduction evidence

The six merged reports' per-scenario evidence (VWO-004 scenario blocks 2, 8,
11, 12; VWO-005 scenario block 6) — pointers in
`docs/validation/reports/SCENARIO-ISSUE-MATRIX.md` §1. VWO-010 did not re-run
these P2/P3 paths (severity below reproduction threshold; static seed/ops
verification only): seed and ops sources confirm each mismatch exists at
0f89393 (e.g. `construction/seed.js` purchase requests; `rideshare/seed.js`
permissions; `media/ops.js` `requestRendition`/channels).

## Fix scope (bounded)

`docs/validation/fixtures/construction/seed.js`,
`docs/validation/fixtures/rideshare/seed.js`,
`docs/validation/fixtures/media/seed.js`,
`docs/validation/fixtures/media/ops-dist.js` (rendition branch),
`docs/validation/scenarios/SCENARIO-CATALOG.md` (wording only where the
fixture is kept as-is),
`docs/validation/fixtures/*/FAILURES.md` (document any new switch binding).

## Acceptance criteria

1. `verify-sweep.sh` → 59/59 after the changes (no regression).
2. Each aligned scenario path runs as catalogued: procurement approve→PO on
   the second OPEN PRQ; support-agent resolution; rendition missing_asset
   switch arms and fires; breaking-news publishes to a push-like fast channel;
   follow-up-note step name matches the surface; hero attach on approved
   story works or the catalog notes the pre-approval requirement.

## Verification commands

```bash
bash docs/validation/fixtures/run-all.sh --reset
bash docs/validation/fixtures/verify-sweep.sh
# re-run the five affected VWO-004 scenarios + VWO-005 media cycle leg
```

## Rollback

Fixture/seed/catalog-only changes; revert the commit. Seeds reset on
`run-all.sh --reset` — no state migration.
