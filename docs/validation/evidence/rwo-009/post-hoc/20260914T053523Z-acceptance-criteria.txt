# RWO-009 acceptance criteria — verification transcript
captured: 2026-09-14T05:35Z on branch rwo-009/upgrade-visibility-gate (fix applied, base 2d37cc0ef7d075e877f370790f82ac9c70df26a4)
surface: codex-rs/workflow-distribution (InMemoryMarketplace engine crate)
cargo/rustc ABSENT in this sandbox: each criterion is verified by (a) the code-path trace
  (before/after transcript 20260914T053523Z-familyI-familyG-before-after-reproduction.txt),
  (b) the predicate-model run (20260914T053523Z-predicate-model-check.txt — real python
  execution of a line-faithful transcription of the Rust control flow, identical scenario
  state before/after), and (c) NEW unit tests in the crate that assert the criterion in
  real Rust — the Tech Lead executes them with `cargo test -p codex-workflow-distribution`
  (the RWO's named command). No compiled-Rust result is claimed in this sandbox.

--------------------------------------------------------------------------------
AC1 — engine attack 6d re-run: foreign installer's evaluate_upgrade returns None
      (or a not-visible refusal); decide_upgrade on a smuggled proposal →
      ReleaseNotVisible; pin unchanged.
--------------------------------------------------------------------------------
setup: bob installs PUBLIC 1.0.0 (follow); acme-ops publishes PRIVATE 2.0.0;
       bob holds license acceptances (access gate is not the hole).

trace: memory.rs evaluate_upgrade (branch lines 456-465) — the candidate filter
       now includes release_installable(&entry.metadata, entry.state,
       &entry.audience, &install.principal): the PRIVATE entry (ownership
       acme-ops ≠ bob; state Private: not publicly discoverable, not Published,
       not Shared-with-bob) fails the predicate → filtered → no candidate → None.
       With a visible newer release present, the proposal targets the newest
       VISIBLE release instead (never the private one).
trace: memory.rs decide_upgrade (branch lines 521-533) — the smuggled proposal
       (2.0.0 ≥ 1.0.0, ordering guard passes) hits entry_for (entry exists) then
       the visibility gate: release_installable(..., &principal) for the install's
       principal bob → false → Err(ReleaseNotVisible { workflow, version }) —
       BEFORE proposal.to.verify()/verified_version disclose any integrity detail.
       Both early-returns fire before self.installs.insert / upgrade_history.push
       → pin unchanged, zero records.

model run (20260914T053523Z-predicate-model-check.txt, attack 6d block):
   BASE : Some(proposal.to = sha256:v2-private-2.0.0) → FIXED: None
   BASE : Ok(applied: True, to: private 2.0.0)          → FIXED: Err(ReleaseNotVisible)
   BASE : pin moved, 1 applied record                   → FIXED: pin unchanged, 0 records
   verdict: PASS (private identity surfaced? BASE=True FIXED=False)

Rust assertions awaiting the Tech Lead's cargo run (src/memory_tests.rs):
   evaluate_upgrade_skips_releases_invisible_to_the_installer — asserts the proposal
     targets the newest VISIBLE release (2.0.0 public, NOT the private 3.0.0), that
     from == the pin, and (positive control) that the visible target still approves.
   evaluate_upgrade_returns_none_when_all_other_releases_are_invisible — asserts None
     when the only newer release is private+foreign, pin unchanged.
   decide_upgrade_refuses_smuggled_proposals_targeting_invisible_releases — builds the
     smuggled UpgradeProposal by hand (PublishedVersionRef::of(&private)), asserts
     Err(ReleaseNotVisible) with exact workflow/version fields, pin unchanged,
     upgrade_history empty. No access grant is seeded for the private release, so an
     UpgradeRefused outcome would prove the guard MISSING — ReleaseNotVisible proves
     the visibility gate fired.

→ PASS (static + model + unit tests encoded; compiled execution deferred to the
  Tech Lead per the RWO's cargo-absent clause).

--------------------------------------------------------------------------------
AC2 — engine attack 12 re-run: older-target upgrade record → refused with the typed
      downgrade error; pin unchanged.
--------------------------------------------------------------------------------
setup: alice installs PUBLIC 2.0.0 (follow); PUBLIC 1.5.0 also published.

trace: memory.rs decide_upgrade (branch lines 512-520) — after the stale check
       (proposal.from == install.installed.version_id, so install.installed IS the
       from-pin) the ordering guard compares proposal.to.identity.semantic_version
       < install.installed.identity.semantic_version: 1.5.0 < 2.0.0 →
       Err(WorkflowDistributionError::IllegalDowngrade { workflow, expected_newer_than:
       2.0.0, got: 1.5.0 }) — the typed error the RWO mandates (error.rs lines
       123-139). The return precedes entry lookup, gates, and every mutation →
       pin unchanged, zero records (refused proposals leave no records, per the
       RWO's rollback note).

model run (predicate-model check, attack 12 block):
   BASE : Ok(applied: True, to: 1.5.0), pin moved 2.0.0 → 1.5.0, 1 applied record
   FIXED: Err(IllegalDowngrade { expected_newer_than: 2.0.0, got: 1.5.0 }),
          pin=sha256:v2-public-2.0.0 unchanged, 0 records
   verdict: PASS

Rust assertions awaiting the Tech Lead's cargo run (src/memory_tests.rs):
   decide_upgrade_refuses_targets_older_than_the_pin — asserts the typed error's
     workflow / expected_newer_than / got fields exactly, pin unchanged, history
     empty. No access grant for the older release → an UpgradeRefused outcome
     would mean the guard is missing; IllegalDowngrade proves it fired.
   rejected_downgrade_proposals_still_record_a_rejection — boundary: explicit
     Reject on a downgrade proposal still records applied=false, to=from (the
     guard guards APPROVAL only; the reject path is untouched).
   equal_semver_targets_are_not_downgrades — boundary: "older than" is strict;
     a same-semver re-seal proceeds through the gates (documents the RWO's
     literal wording).

→ PASS (static + model + unit tests encoded; compiled execution deferred to the
  Tech Lead per the RWO's cargo-absent clause).

--------------------------------------------------------------------------------
AC3 — all existing distribution tests still pass (search/install visibility,
      gate order, stale-proposal, AlreadyInstalled).
--------------------------------------------------------------------------------
The fix touches exactly one engine crate and no frozen crate. Every existing
test in tests/e2e_workflow_distribution.rs and the in-crate unit tests
(state_tests.rs, metadata_tests.rs) were traced call-by-call against the new
behavior (full table in 20260914T053523Z-verify-sweep.txt):
  - publishing_seals_releases_and_rejects_every_tampered_record — no upgrade-path
    calls; publish/fetch semantics untouched → unaffected.
  - forking_derives_new_immutable_versions_with_lineage_provenance — fork path
    untouched (frozen semantics: fork is RWO-016/Family N surface) → unaffected.
  - installs_are_license_gated_with_deny_and_allow_paths — install path untouched
    (AlreadyInstalled behavior intact) → unaffected.
  - entitlement_denial_gates_installation_only_and_never_mutates_semantics —
    evaluate_upgrade (public 2.0.0, visible to bob) still surfaces it;
    decide_upgrade: ordering guard passes (2.0.0 > 1.0.0), visibility passes
    (Public), gates refuse → Err(UpgradeRefused) exactly as asserted.
  - pin_stays_pinned_and_follow_advances_only_with_explicit_approval — pin
    policy → None; follow 1.0.0 → 1.1.0 public: surfaces, Reject records
    (applied=false, to=from), Approve applies (ordering 1.1.0 > 1.0.0 passes,
    visibility Public passes, gates seeded-pass), re-decide → StaleUpgradeProposal,
    NotInstalled for uninstalled workflow — every asserted outcome identical.
  - search_filters_by_every_facet_and_visibility_scope — search/discovery
    untouched → unaffected.
  - no_credentials_appear_in_any_distribution_record — the serialized
    evaluate_upgrade result is still Ok(None) (only one release, excluded by
    version_id) → unaffected.
  - marketplace_installs_hand_off_to_the_trigger_plane_... — install/hand-off
    only → unaffected.
  - state_tests / metadata_tests — modules untouched → unaffected.
  No existing test performs an older-target decide or a private-target decide,
  so no asserted outcome changes anywhere in the suite.

→ PASS by exhaustive static call-site trace; compiled confirmation deferred to
  the Tech Lead (cargo test -p codex-workflow-distribution).

--------------------------------------------------------------------------------
probe battery rider (RWO verification comment): attacks 5b/6d/12 → expect REFUSED
--------------------------------------------------------------------------------
6d: REFUSED (AC1 above — evaluate None, decide ReleaseNotVisible, pin unchanged).
12: REFUSED at the decision/record layer (AC2 above — typed IllegalDowngrade, pin
    unchanged). Bounded-scope note: evaluate_upgrade still surfaces the older
    release as pure data — the RWO's evaluate item adds only the visibility
    filter; the refusal (typed error) lives on decide_upgrade, which is what the
    acceptance criterion mandates ("older-target upgrade record → refused").
5b (fork misattribution): NOT closed by this RWO — it is Family N (VWO-009 F9),
  owned by RWO-016 per docs/validation/reports/SCENARIO-ISSUE-MATRIX.md line 231
  ("follow-up — RWO-016 signed/derived attribution content"). RWO-009's fix scope
  (memory.rs upgrade path) cannot and does not touch forge attribution. Recorded
  as a deviation/question in the completion report: the RWO's verification comment
  lists 5b, but its own "Closes" line and the matrix assign 5b elsewhere.
