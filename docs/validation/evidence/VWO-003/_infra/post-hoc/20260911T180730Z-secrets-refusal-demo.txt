VWO-003 harness self-test — secrets-refusal (fail-closed) demo, REDACTED transcript
date: 2026-09-11T18:07Z (UTC)

Input file (synthetic, deliberately contaminated — values already redacted
here per evidence/README.md §5; the raw file lived only in /tmp and was
never written into the evidence tree):

  login response example:
  { "token": "ghp_<REDACTED-EXAMPLE-TOKEN>", "ok": true }
  password: <redacted>

Command:
  bash docs/validation/scripts/capture-evidence.sh VWO-003 _infra post-hoc \
       /tmp/contaminated.txt bad

Observed output (token value redacted in this transcript; the live run
printed the offending lines verbatim so the worker knows exactly what to
fix):

  capture-evidence: REFUSED — secret-looking content detected (credential patterns).
  Redact these lines (evidence/README.md §5: token=<redacted>, password: <redacted>) and re-capture:
  2:{ "token": "ghp_<…>", "ok": true }
  3:password: hunter<…>
  exit code: 1
  (nothing written to docs/validation/evidence/)

Post-condition verified: the evidence directory contains ONLY the clean
capture from the same session (20260911T180656Z-harness-self-test.txt);
the contaminated artifact was never written. The refusal itself is the
enforcement oracle for the credentials/secrets ban (VWO-003 Forbidden list;
REPORT-SCHEMA.md §13; evidence/README.md §5).
