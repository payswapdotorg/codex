# RWO-001 environment identity
# Captured: 2026-09-13T02:28:38Z (UTC)

## Repository
- clone: https://github.com/payswapdotorg/codex (public, anonymous)
- base: main @ 2d37cc0ef7d075e877f370790f82ac9c70df26a4 (verified: git rev-parse 2d37cc0ef7d075e877f370790f82ac9c70df26a4^{commit} -> 2d37cc0ef7d075e877f370790f82ac9c70df26a4)
- branch: rwo-001/mount-control-plane (created at the verified base; see post-hoc/branch-and-diff.txt for the head SHA after the work commit)

## Toolchain probe (exact commands and results)
$ which cargo
    (no output — cargo/rustc ABSENT in this sandbox)
$ which rustc
    (no output)
$ which node
    /usr/bin/node
$ node --version
    v24.19.0
$ which git
    /usr/bin/git
$ git --version
    git version 2.47.3
$ which rg
    /usr/bin/rg
$ rg --version
    ripgrep 14.1.1
$ python3 --version
    Python 3.12.14 (python-zstandard 0.25.0 installed for the precomputed-export artifact work)

## Consequence (honest limitation)
cargo test / cargo clippy / cargo fmt COULD NOT be run in this sandbox.
Every Rust-side claim in this evidence tree is verified by static source
analysis (probe inversions, byte-exact artifact regeneration with structural
verification, careful type/borrow review), and the precomputed experimental
protocol export was regenerated by a python replication of the
ts-rs/schemars generator behavior that `just write-app-server-schema` would
perform, with the diff verified to be exactly the RWO-001 additions.
The Tech Lead independently compiles and runs everything
(docs/validation/VALIDATION-PROGRAM.md section 11 step 6).
