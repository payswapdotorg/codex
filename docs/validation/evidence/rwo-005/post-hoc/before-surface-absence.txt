# RWO-005 post-hoc: the absent fork + lineage surface at base (before reproduction)
# base: main @ 9441a823f576a53591a6ce3a96a5ed47b6e9ca85 (branch rwo-005/fork-lineage-surface, clean tree)
# Captured: 2026-09-14T18:30:08Z

## probe 1 — the CLI workflow group has no fork subcommand
\$ rg -n "Fork" codex-rs/cli/src/workflow_cmd.rs
    (no matches — no fork subcommand, no fork rendering)

\$ rg -n "pub enum WorkflowSubcommand" -A 6 codex-rs/cli/src/workflow_cmd.rs
37:pub enum WorkflowSubcommand {
38-    /// Teach a workflow, compile it, review it, approve it, and publish
39-    /// an immutable version — one guided invocation.
40-    Teach(TeachArgs),
41-    /// Operate durable workflow instances.
42-    Instance(InstanceCommand),
43-}

## probe 2 — the protocol has no workflow/fork method
\$ rg -n "=> \"workflow/" codex-rs/app-server-protocol/src/protocol/common.rs
551:    WorkflowTeachStart => "workflow/teach/start" {
559:    WorkflowTeachInstruct => "workflow/teach/instruct" {
568:    WorkflowTeachDemonstrate => "workflow/teach/demonstrate" {
577:    WorkflowTeachReconcile => "workflow/teach/reconcile" {
585:    WorkflowCompile => "workflow/compile" {
593:    WorkflowReview => "workflow/review" {
601:    WorkflowApprove => "workflow/approve" {
609:    WorkflowPublish => "workflow/publish" {
617:    WorkflowInstanceRun => "workflow/instance/run" {
625:    WorkflowInstanceList => "workflow/instance/list" {
633:    WorkflowInstanceGet => "workflow/instance/get" {
641:    WorkflowInstanceResume => "workflow/instance/resume" {
649:    WorkflowInstanceCancel => "workflow/instance/cancel" {

\$ rg -n "WorkflowFork|workflow/fork" codex-rs/app-server-protocol/src/
    (no matches — the fork port is not exposed through the protocol)

## probe 3 — the app-server mount has no fork handler
\$ rg -n "fn fork|fork_repository|carried_attribution|ForkRequest" codex-rs/app-server/src/workflow.rs codex-rs/app-server/src/message_processor.rs
    (no matches — the control plane never calls the engine fork port)

\$ rg -n "codex-workflow-distribution|codex-workflow-forge" codex-rs/app-server/Cargo.toml
    (no matches — the app-server does not even depend on the fork-port crates)

## probe 4 — the engine fork port EXISTS but is library-only (the VWO-010 Family C root cause)
\$ rg -n "fn fork" codex-rs/workflow-distribution/src/port.rs codex-rs/workflow-distribution/src/memory.rs codex-rs/workflow-forge/src/memory.rs
codex-rs/workflow-forge/src/memory.rs:369:    pub fn fork(
codex-rs/workflow-distribution/src/port.rs:304:    fn fork(
codex-rs/workflow-distribution/src/memory.rs:275:    fn fork(

\$ rg -n "forks_must_carry_upstream_attribution" codex-rs/workflow-distribution/src/metadata_tests.rs
140:fn forks_must_carry_upstream_attribution() {

## probe 5 — no fork/lineage types in the protocol surface
\$ rg -n "WorkflowForkLineage|WorkflowForkAttribution" codex-rs/app-server-protocol/src/protocol/v2/workflow.rs
    (no matches — no lineage data model on the product surface)

## conclusion — VWO-005 F4 / VWO-009 F6 (fork half) REPRODUCED at base: the engine
## implements fork + lineage (probe 4) but no product surface mounts it (probes 1-3, 5).
