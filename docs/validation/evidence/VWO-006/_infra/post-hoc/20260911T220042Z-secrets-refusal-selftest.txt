VWO-006 — capture-evidence.sh secrets-refusal verification (keep-it-that-way check)
Date (UTC): 2026-09-11T22:00:35Z

Attempt: capture an artifact containing fake live-looking credentials (AWS-style key + JWT-shaped bearer + api_key assignment):

--- input artifact:
POST /api/login
authorization: Bearer <redacted JWT-shaped fake>
api_key: <redacted AWS-style fake>
--- command:
bash docs/validation/scripts/capture-evidence.sh VWO-006 _infra post-hoc /tmp/secret-probe.txt secret-refusal-probe
--- result:
capture-evidence: REFUSED — secret-looking content detected (credential patterns).
Redact these lines (evidence/README.md §5: token=<redacted>, password: <redacted>) and re-capture:

2:authorization: Bearer <redacted JWT-shaped fake>
3:api_key: <redacted AWS-style fake>
script exit code: 1 (expect 1 = refused)

--- verification that nothing was written:
no docs/validation/evidence/VWO-006/_infra directory exists — the refusal wrote NOTHING (fail-closed held)

CONCLUSION: capture-evidence.sh still refuses secret-looking content before writing (fail-closed). Kept that way.
